<h1 class="fbr-page-header mb-4"><i class="fas fa-life-ring text-primary"></i> Support Tickets</h1>

<%= form_with url: admin_support_tickets_path, method: :get, local: true, html: auto_filter_form_options(html_class: 'mb-4 d-flex flex-wrap gap-2 align-items-center') do |f| %>
  <%= hidden_field_tag :page, 1 %>
  <%= f.select :status, options_for_select([['All', ''], *SupportTicket::STATUSES.map { |s| [s.humanize, s] }], params[:status]), {}, class: 'form-select w-auto', **auto_filter_change_options %>
  <% if params[:status].present? %>
    <%= link_to 'Clear', admin_support_tickets_path, class: 'btn btn-outline-secondary btn-sm' %>
  <% end %>
<% end %>

<div class="card">
  <table class="table table-hover mb-0">
    <thead><tr><th>Subject</th><th>User</th><th>Status</th><th>Priority</th><th>Updated</th><th></th></tr></thead>
    <tbody>
      <% @tickets.each do |ticket| %>
        <tr>
          <td><%= ticket.subject %></td>
          <td><%= ticket.user.email %></td>
          <td><span class="badge bg-secondary"><%= ticket.status %></span></td>
          <td><%= ticket.priority %></td>
          <td class="small"><%= ticket.updated_at.strftime('%d %b %Y') %></td>
          <td><%= link_to 'View', admin_support_ticket_path(ticket), class: 'btn btn-sm btn-outline-primary' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<%= paginate @tickets if defined?(paginate) %>
